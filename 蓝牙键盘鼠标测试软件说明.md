# 蓝牙鼠标测试软件说明

> 项目名称：ESP32-S3 蓝牙鼠标键盘（BLE HID 复合设备）
> 平台：ESP32-S3（Xtensa，支持 WROOM-1-N4R2：4MB Flash + 2MB PSRAM，亦兼容 N16R8：16MB Flash + 8MB PSRAM；sdkconfig 按 4MB Flash 配置，双规格通吃）
> 开发框架：ESP-IDF v5.3.5
> 蓝牙协议栈：Bluedroid + BLE（ESP32-S3 不支持经典蓝牙 BR/EDR，仅支持 BLE）
> HID 实现：ESP-IDF 通用 `esp_hid` 组件（`esp_hidd.h` / `esp_hid_common.h`），传输层 `ESP_HID_TRANSPORT_BLE`

---

## 1. 软件概述

本软件将 ESP32-S3 实现为一个**免驱的蓝牙 HID 复合设备**：电脑在蓝牙设置中配对一次后，设备被自动识别为"鼠标 + 键盘"，无需安装任何驱动。

设备的核心能力是**随机动作模拟**：配对并按下 KEY2 启动后，按预设概率随机抽取并执行一组拟人化的鼠标 / 键盘动作（拖拽、点击、滚轮、方向键、休息），循环不停；按下 KEY0 立即停止并释放所有按键；KEY1+KEY3 长按 3 秒重置蓝牙配对。GPIO1 上的状态 LED 在执行动作时常亮（低电平），停止时熄灭（高电平）。

整个过程通过串口日志可视化，便于调试与验收。

---

## 2. 总体架构

软件基于 FreeRTOS 多任务，划分为 6 个模块。各模块职责清晰、解耦，通过统一的 **HID 发送队列** 完成并发安全的数据上报。

```
                         app_main (main.c)
                              │
        ┌─────────────┬───────┼───────────────┬──────────────┐
        ▼             ▼       ▼               ▼              ▼
   NVS 初始化    led_status  hid_sender    ble_hid      keys / action_engine
   (配对存储)   (状态LED)    (串行发送)    (GAP+HIDD)    (控制面/生产者)
                              ▲              │
                              │              │ esp_hidd_dev_input_set
                              │              ▼
                    ┌─────────┴─────┐   电脑(主机)
                    │ FreeRTOS 队列 │
                    └───────────────┘
```

### 模块职责一览

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| 程序入口 | `main.c` | 初始化 NVS，按序启动各模块任务，打印启动横幅 |
| 状态 LED | `led_status.c/h` | GPIO1 普通输出：动作执行中亮（低电平）/ 停止时灭（高电平） |
| HID 核心 | `ble_hid.c/h` | 复合 HID 报告描述符、HIDD 初始化、事件回调、连接状态、发送队列与发送任务、完整鼠标发送接口、蓝牙重置 |
| GAP 封装 | `esp_hid_gap.c/h` | 从官方示例复用的 GAP 实现：控制器/协议栈初始化、BLE 广播配置（HID UUID + Appearance）、配对安全参数、随机地址轮换 |
| 按键控制面 | `hid_keys.c/h` | XL9555 初始化、20ms 轮询消抖、KEY2 启动 / KEY0 停止边沿检测、KEY1+KEY3 组合键重置 |
| 动作引擎 | `action_engine.c/h` | 按概率随机调度 5 类动作、鼠标复位子过程、可中断延迟、按键强制释放、状态 LED 控制 |

---

## 3. 启动流程

`main.c` 的 `app_main()` 依次完成以下初始化（顺序经过设计，避免依赖缺失）：

1. **`init_nvs()`** — 初始化 NVS（非易失存储）。BLE 配对绑定信息（LTK 等）存放于此。当检测到 `ESP_ERR_NVS_NO_FREE_PAGES` / `ESP_ERR_NVS_NEW_VERSION_FOUND` 时自动擦除重建。
2. **`led_status_init()`** — 配置 GPIO1 为推挽输出并初始化为灭（高电平）。
3. **`hid_sender_start()`** — 创建 HID 发送队列与串行发送任务。**必须在生产者任务（按键/动作引擎）启动之前创建**，否则报文无处投递。
4. **`ble_hid_init()`** — 初始化 GAP + HIDD，配置复合报告描述符，开始广播，等待主机配对。
5. **`action_engine_start_task()`** — 创建动作引擎 EventGroup 与任务，初始为停止态。**必须在按键任务之前创建**，否则按键回调会操作未初始化的 EventGroup。
6. **`keys_start()`** — 初始化 XL9555 并启动按键扫描任务（控制面）。

启动完成后，串口提示用户：
> 请在电脑上打开 蓝牙设置 → 添加设备 → 蓝牙，选择 "ESP32-S3 KM" 进行配对
> 配对后：按 KEY2 启动动作 / 按 KEY0 停止 / KEY1+KEY3 长按3秒重置蓝牙

---

## 4. 调用的关键接口

### 4.1 ESP-IDF 蓝牙 / HID 接口

| 接口 | 用途 |
| --- | --- |
| `nvs_flash_init()` / `nvs_flash_erase()` | 初始化/擦除配对存储 |
| `esp_hid_gap_init()` | 初始化蓝牙控制器与 Bluedroid 协议栈 |
| `esp_hid_ble_gap_adv_init(appearance, name)` | 配置 BLE 广播（含 HID Service UUID 0x1812 与 Appearance） |
| `esp_hid_ble_gap_adv_start()` | 开始广播 |
| `esp_hidd_dev_init()` | 注册 HID 设备（传入报告描述符、厂商名、VID/PID），启动 HID over GATT |
| `esp_hidd_dev_input_set()` | 发送 HID 输入报文（鼠标/键盘） |
| `esp_ble_gap_set_rand_addr()` | 重置时轮换随机地址，使旧配对失效 |
| `esp_ble_gap_disconnect()` | 主动断开指定对端 |
| `esp_ble_gap_stop_advertising()` | 停止广播 |

### 4.2 本软件对外接口（见 `ble_hid.h` / `hid_keys.h` / `action_engine.h` / `led_status.h`）

| 接口 | 说明 |
| --- | --- |
| `esp_err_t ble_hid_init(void)` | 初始化 BLE 协议栈 + HID 设备并开始广播 |
| `bool ble_hid_is_connected(void)` | 查询当前是否已与主机建立 HID 连接 |
| `esp_err_t hid_sender_start(void)` | 启动 HID 报文串行发送任务（内部创建队列） |
| `esp_err_t ble_hid_send_mouse(int8_t dx, int8_t dy)` | 投递一次鼠标相对移动（非阻塞，兼容原接口） |
| `esp_err_t ble_hid_send_mouse_full(int8_t dx, int8_t dy, int8_t wheel, uint8_t buttons)` | 投递完整鼠标报文（位移+滚轮+按键，非阻塞） |
| `esp_err_t ble_hid_send_mouse_full_blocking(...)` | 同上，但队列满时阻塞等待最多 100ms（供分片位移使用） |
| `esp_err_t ble_hid_send_key(uint8_t keycode, bool pressed)` | 投递一次键盘按下/松开（非阻塞） |
| `esp_err_t ble_hid_reset(void)` | 重置蓝牙配对状态（断链 + 删 bond + 换地址 + 重广播） |
| `esp_err_t action_engine_start_task(void)` | 创建引擎 EventGroup 与任务，初始为停止态 |
| `void action_engine_start(void)` | KEY2：置运行位，开始执行动作 |
| `void action_engine_stop(void)` | KEY0：停止动作并释放所有按键 |
| `bool action_engine_stop_and_wait(uint32_t timeout_ms)` | 停止并等待引擎确认已释放按键（供重置前调用） |
| `esp_err_t led_status_init(void)` | 初始化 GPIO1 状态 LED（初始灭） |
| `void led_status_set(bool on)` | 设置 LED：true=亮（低电平），false=灭（高电平） |
| `esp_err_t keys_start(void)` | 初始化 XL9555 并启动按键扫描任务 |

---

## 5. 实现的功能

### 5.1 蓝牙 HID 复合设备

- 设备名称：`BLE_KM`（广播中显示，便于在电脑配对列表识别）。
- 广播数据包含 **HID Service UUID 0x1812** 与 **Appearance**，电脑端自动识别为标准键盘 + 鼠标。
- 配对采用 **Just Works（免输密码）**：IO Capability 设为 `ESP_IO_CAP_NONE`，并开启带 Bonding 的模式，配对信息持久保存于 NVS，断电后仍能自动重连。
- 断开连接后自动恢复广播。

#### 复合 HID 报告描述符

单个 Report Map 内含两个 Top-Level Collection，用 Report ID 区分：

| Report ID | 设备 | 报文长度 | 格式 |
| --- | --- | --- | --- |
| 1 | 鼠标 | 4 字节 | buttons(1) + X(int8) + Y(int8) + wheel(int8) |
| 2 | 键盘 | 8 字节 | modifier(1) + reserved(1) + keycode[6] |

- 鼠标 X/Y 为**相对位移**（Logical Range -127~127），满足每步 10 计数的平滑移动需求。
- 键盘报文：`buf[0]`=修饰键，`buf[1]`=保留，`buf[2..7]`=按键码；按下时写入键值，松开时全 0。

### 5.2 随机动作引擎

动作引擎（`action_engine.c/h`）是改造后的核心。默认处于停止态，按下 **KEY2** 后开始，按概率随机抽取并执行动作，循环不停；按下 **KEY0** 立即停止。

#### 概率与动作清单（权重合计不必为 100，仅相对比例决定分布；0 表示不参与抽取）

| 动作 | 权重 | 内容 |
| --- | --- | --- |
| 模拟鼠标拖拽 | 0 | 按住左键 → 在活动范围内随机抽目标点、分片步进拖过去（距离 20~100，步进 10~30），重复 1~5 次，每次间隔 600~1500ms → 松开左键 → 鼠标复位 → 随机延迟 500~5000ms |
| 模拟鼠标点击 | 50 | 在活动范围内随机抽目标点、分片步进移过去 → 单击左键（按下保持 20~250ms 松开），重复 1~10 次，每次间隔 100~1000ms → 鼠标复位 → 随机延迟 1000~5000ms |
| 模拟鼠标滚轮 | 50 | 先随机移动到目标点 → 随机上下滚轮(步进 1~8)，重复 1~5 次，每次间隔 100~500ms → 滚轮归位(累计和归零) → 鼠标复位 → 随机延迟 1000~5000ms |
| 模拟键盘方向键 | 30 | 随机敲击上/下/左/右方向键，重复 1~20 次，每次间隔 50~800ms → 随机延迟 1000~5000ms |
| 模拟休息 | 35 | 随机延迟 1~20 秒 |
| 模拟滑动鼠标 | 50 | 随机方向滑动随机距离（距离 10~30，步进 1~15），重复 1~20 次，每次间隔 100~500ms → 鼠标复位 → 随机延迟 500~1000ms |
| 打字（随机单词） | 0 | 从单词表随机抽词逐字符发送（字符间 40~700ms），每词后加空格（40~1000ms），词数 1~5，词间间隔 500~2000ms → 随机延迟 500~1200ms |
| 切换程序（Alt+Tab） | 0 | 按住 Alt 点 Tab 切换窗口；切换次数 0~1（0=本次不切换），每次按键间隔 500~1000ms，动作后休息 700~1500ms |

- 每个动作**自带的随机延迟之外，额外追加 100ms 保护延迟**（`ACTION_GUARD_DELAY_MS`）才进入下一个动作。
- **鼠标复位**（公共子步骤）：BLE 鼠标是相对位移协议，读不到光标绝对位置；且 Windows 对 HID 相对位移有指针加速，按“半屏几何”回中心不可靠（推/回不抵消）。实测 1080@125% 与 2560@100% 落点“感觉差不多、都偏出屏幕”，印证与屏幕数值无关、是系统加速导致。故复位改为**经验值两步法**：① 朝 `MOUSE_HOME_CORNER` 配置角落方向发 `MOUSE_HOME_PUSH_PX`（分片），只要足够大，真实光标越过边界被系统截停在角落（绝对已知点）；② 朝撞角反方向发 `MOUSE_HOME_BACK_X` / `MOUSE_HOME_BACK_Y`（经验常量，X/Y 独立，因为屏幕宽高比≠1 且系统对 X/Y 加速可能不同），决定最终落点。各值均为“发送的位移计数”而非物理像素，需按实测落点调校（见下方调参）。`MOUSE_HOME_CORNER` 决定撞哪个角。
- 所有随机区间、概率权重、屏幕分辨率均在 `action_engine.h` 中以宏定义，便于调参（详见 §5.8 宏定义说明）。
- **可中断性**：所有延迟与循环均基于 `action_delay_ms()`（底层 `xEventGroupWaitBits`），KEY0 或蓝牙断开时最多在「一次分片延迟（约 10ms）」内被唤醒。动作退出时统一调用 `action_release_all()`，强制发送 `buttons=0` 鼠标报文 + 全 0 键盘报文，杜绝左键/按键卡死残留。
- 方向换算使用 **Q15 定点正弦表（64 点）**，不引入浮点 `sinf/cosf`，规避 libm 依赖。
- 大位移（>127）按 ≤120 计数分片发送，并用**阻塞投递**（`ble_hid_send_mouse_full_blocking`）保证轨迹完整，避免队列满丢帧。

### 5.3 按键控制面（启停 + 重置）

- 硬件：4 个按键挂在 **XL9555 I2C 扩展芯片**的 P4~P7，另一端接 GND，按下为低电平；XL9555 内部上拉，松开读为高电平。读回字节位：`Key0→bit3(P1_7)`、`Key1→bit2(P1_6)`、`Key2→bit1(P1_5)`、`Key3→bit0(P1_4)`。
- 本版 **不再** 将按键映射为方向键（方向键仅由"动作4"自动模拟）。按键改为纯控制面：

| 按键 | XL9555 引脚 | 功能 |
| --- | --- | --- |
| Key0 | P1_7 | 停止（KEY0）：下降沿触发，立即停止动作并释放全部按键 |
| Key1 | P1_6 | **短按** → 运行额度 +10 分钟（LED 闪一下确认）；与 Key3 长按 3 秒 → 重置蓝牙；若长按松开后 5 秒内再次长按 3 秒 → 额外恢复出厂（账号+参数） |
| Key2 | P1_5 | 启动（KEY2）：下降沿触发，仅在停止态下生效 |
| Key3 | P1_4 | 短按下降沿 → **停止动作并复位鼠标**（供确认落点，**停止态下同样执行**，蓝牙未连接时仅停动作）；与 Key1 长按 3 秒 → 重置蓝牙（组合键） |
| BOOT | GPIO0 | 板载 BOOT 键（独立于 XL9555，始终可用）：**短按** → 切换动作启停（运行↔停止）；**长按 ≥3 秒** → 重置蓝牙；若松开后 5 秒内再次长按 3 秒 → 恢复出厂（账号+参数），效果等同 KEY1+KEY3 组合键 |

- 按键扫描采用 **20ms 周期轮询**（同时起到软件消抖作用），KEY2/KEY0/KEY3 使用**下降沿**（先松开后按下）触发一次，不做长按连发，避免误触发多次启停。
- 组合键（KEY1+KEY3 长按 3 秒）成立期间屏蔽 KEY3 的单按复位判定，防止重置过程中误触发复位；同时屏蔽 KEY0/KEY2 的边沿判定，防止误启停。

### 5.4 蓝牙重置（组合键）

- 触发：**上（Key3）+ 下（Key1）同时长按 `KEY_COMBO_RESET_MS`（3000ms）**。
- 重置内容（`ble_hid_reset()`）：
  1. 停止当前广播；
  2. 若已连接，主动断开当前主机；
  3. 删除全部已绑定主机的配对记录；
  4. **轮换一个全新随机蓝牙地址**（见 5.5）；
  5. 清空发送队列残留；
  6. 重新开启广播，等待新设备配对。
- 组合键成立期间屏蔽上/下两键的方向键上报，避免误发一串方向键。

### 5.5 重置时的随机地址轮换（重连风暴防护）

**问题背景**：仅删除本端配对记录时，Windows 仍保存旧配对，会不断自动重连；重连后本端 LTK 已删，加密失败（`ltk_req_neg_reply`）→ 断开 → 再重连，形成重连风暴（日志：`bta_dm_set_encryption, not find peer_bdaddr`）。

**解决方案**：商业无线键鼠的通用做法——重置时调用 `esp_ble_gap_set_rand_addr()` 换一个**静态随机地址**（地址最高 2 bit 强制为 `11` 以符合规范）。旧配对对应老地址自然失效，Windows 将其视为全新设备重新配对，重连风暴消失。

`esp_hid_gap.c` 提供 `esp_hid_ble_use_new_random_addr()` 封装此逻辑；`ble_hid_reset()` 在停止广播后、重启广播前调用它。同时，`esp_hid_gap.c` 的 `ESP_GAP_BLE_AUTH_CMPL_EVT` 处理中，对**加密失败**的对端主动 `esp_ble_gap_disconnect()`，作为防御性兜底。

> 注意：重置后 Windows 蓝牙列表中会出现两个同名设备（旧失效记录 + 新地址）。旧的可在系统中"删除/忘记此设备"清理，不删也不会再重连风暴。

### 5.6 LED 指示（状态灯）

- 使用 **GPIO1 普通输出**驱动的 LED（低电平亮 / 高电平灭）。
- 动作引擎进入运行态且蓝牙已连接时点亮（低电平）；停止或等待时熄灭（高电平）。
- 不再有周期闪烁行为，仅作为"是否正在执行动作"的状态灯。`led_status.c` 直接调用 `gpio_set_level()`，不依赖 `led_strip` / RMT 组件，无需 WS2812 灯带。

### 5.7 串口日志输出

- 蓝牙启动、广播开始、配对过程、连接成功、断开（含原因）等状态（`ESP_LOGI`）。
- 每次抽中的动作打印一行汇总（动作名 + 关键随机参数）（`ESP_LOGI`）。
- 启动 / 停止 / 组合键重置 / 动作被中断释放等关键状态（`ESP_LOGI/W`）。
- 发送失败 / 队列拥塞：告警（`ESP_LOGW`）；分片位移、单步等细节用 `ESP_LOGD`（默认不输出，调高日志级别可见）。

### 5.8 宏定义说明（调参指南）

所有可调参数集中在 **`main/action_engine.h`**，以 `#define` 给出，修改**只需改这一个文件**，无需动逻辑代码。每个宏都用 `#ifndef … #endif` 包裹，因此也可以在 `sdkconfig`、`CMakeLists.txt` 的 `add_definitions()` 或编译命令 `-D` 中覆盖（例如 `-DMOUSE_POS_LIMIT_X=800`）。**改完宏后必须重新编译并烧录**，宏值在编译期固化进固件，运行时无法更改。

#### 5.8.1 宏分类与含义

| 类别 | 宏 | 含义 | 默认 |
| --- | --- | --- | --- |
| 屏幕与缩放 | `SCREEN_W` / `SCREEN_H` | 主屏物理分辨率，仅用于动作内“活动范围”折算。**复位已改为经验值，不再依赖屏幕几何** | 2560 / 1440 |
| | `SCREEN_SCALE_PCT` | 系统显示缩放百分比；有效活动范围按 `基准 × 100 / SCALE` 自动缩小，避免真实光标被推得过远 | 125 |
| 活动范围 | `MOUSE_POS_LIMIT_X` / `MOUSE_POS_LIMIT_Y` | 相对屏幕中心的活动半范围基准（像素），默认 ±400 / ±200 | 400 / 200 |
| | `MOUSE_POS_EFF_X` / `_EFF_Y` | **派生宏** = `LIMIT × 100 / SCALE`，实际生效范围；不要直接改，改上游即可 | 自动 |
| 复位撞角 | `MOUSE_HOME_CORNER` | 复位撞哪个角：`CORNER_TOP_RIGHT` / `CORNER_TOP_LEFT` / `CORNER_BOTTOM_RIGHT` / `CORNER_BOTTOM_LEFT` | `CORNER_TOP_RIGHT` |
| 复位经验值 | `MOUSE_HOME_PUSH_PX` | 撞角朝角落方向发的总位移计数（要足够大确保越过边界被截停） | 2000 |
| | `MOUSE_HOME_BACK_X` | 从角落朝中心回发的水平位移计数（决定水平落点，需实测调校） | 400 |
| | `MOUSE_HOME_BACK_Y` | 从角落朝中心回发的垂直位移计数（决定垂直落点，1080@125% 屏实测 200 居中） | 200 |
| 概率权重 | `ACT_W_DRAG` / `CLICK` / `WHEEL` / `ARROW` / `REST` / `MOVE` / `WORD` / `ALT_TAB` | 八类动作抽取权重（合计不必为100，仅相对比例；0=不参与抽取） | 0 / 50 / 50 / 30 / 35 / 50 / 0 / 0 |
| 动作参数 | `DRAG_*` / `CLICK_*` / `WHEEL_*` / `ARROW_*` / `REST_*` / `MOVE_*` / `WORD_*` / `ALT_TAB_*` | 拖拽/点击/滚轮/方向键/休息/滑动/打字/切换程序 各自重复次数、间隔、步进等随机区间（见下表） | 见下 |
| 时序 | `ACTION_GUARD_DELAY_MS` | 动作间额外保护延迟 | 100 |
| | `ACTION_MOVE_CHUNK_MAX` | 分片单帧最大相对位移计数 | 25 |
| | `ACTION_MOVE_STEP_MS` | 分片移动帧间延迟 | 28 |
| | `ACTION_HOME_STEP_MS` | 复位撞角帧间延迟 | 12 |

#### 5.8.2 动作参数宏（当前生效值）

| 宏（前缀） | 参数 | 当前值 |
| --- | --- | --- |
| `DRAG_REPEAT` | 重复次数 | 1~5 |
| `DRAG_INTERVAL` | 间隔(ms) | 600~1500 |
| `DRAG_END_DELAY` | 结束延迟(ms) | 500~5000 |
| `DRAG_DIST_*` / `DRAG_STEP_*` | 距离/步长（**预留，当前未直接使用**） | 20~100 / 1~20 |
| `CLICK_REPEAT` | 重复次数 | 1~10 |
| `CLICK_INTERVAL` | 间隔(ms) | 100~1000 |
| `CLICK_HOLD` | 左键保持(ms) | 20~250 |
| `CLICK_END_DELAY` | 结束延迟(ms) | 1000~5000 |
| `CLICK_STEP_*` | 步长（**预留，当前未直接使用**） | 1~30 |
| `WHEEL_REPEAT` | 重复次数 | 1~5 |
| `WHEEL_INTERVAL` | 间隔(ms) | 100~500 |
| `WHEEL_TICK` | 滚轮步进 | 1~8 |
| `WHEEL_END_DELAY` | 结束延迟(ms) | 1000~5000 |
| `WHEEL_STEP_*` | 步长（**预留，当前未直接使用**） | 1~30 |
| `ARROW_REPEAT` | 重复次数 | 1~20 |
| `ARROW_INTERVAL` | 间隔(ms) | 50~800 |
| `ARROW_END_DELAY` | 结束延迟(ms) | 1000~5000 |
| `REST_DELAY` | 休息延迟(ms) | 1000~20000 |
| `MOVE_REPEAT` | 重复次数 | 1~20 |
| `MOVE_INTERVAL` | 间隔(ms) | 100~500 |
| `MOVE_END_DELAY` | 结束延迟(ms) | 500~1000 |
| `WORD_REPEAT` | 打字词数 | 1~5 |
| `WORD_CHAR_DELAY` | 字符间延迟(ms) | 40~700 |
| `WORD_SPACE_DELAY` | 空格延迟(ms) | 40~1000 |
| `WORD_INTERVAL` | 词间间隔(ms) | 500~2000 |
| `WORD_END_DELAY` | 结束延迟(ms) | 500~1200 |
| `ALT_TAB_REPEAT` | 切换次数（0=不切换，页面可放宽至 0~999） | 0~1 |
| `ALT_TAB_INTERVAL` | 按键间隔(ms，页面可放宽至 50~5000) | 500~1000 |
| `ALT_TAB_END_DELAY` | 动作后休息(ms，页面可放宽至 50~60000) | 700~1500 |

> 注：当前动作实现采用“随机目标点 + 受限步进移动”，因此 `DRAG_DIST_*` / `DRAG_STEP_*` / `CLICK_STEP_*` / `WHEEL_STEP_*` 这类“距离/步长”宏暂未被动作代码引用，保留以备后续改回角度/距离式移动；改动它们当前不会改变行为。

#### 5.8.3 常见调参场景

- **改动作节奏/概率**：调 `ACT_W_*` 与对应 `REPEAT`/`INTERVAL`；想整体更慢就调大各 `INTERVAL_MIN` 与 `ACTION_MOVE_STEP_MS`。
- **适配缩放屏**：把 `SCREEN_SCALE_PCT` 设为系统实际缩放值（如 125、150）。
- **限制活动范围**：改 `MOUSE_POS_LIMIT_X/Y`（相对屏幕中心的半范围）。
- **复位落点偏到角落（回多了）**：说明 `MOUSE_HOME_BACK_X/Y` 给大了，调小；偏撞角反方向（回少了）则调大。撞角没到边界（整体偏撞角反方向）则 `MOUSE_HOME_PUSH_PX` 给小了，调大。各值均为发送计数，需实测收敛；改完务必 `fullclean` 后重编译烧录。
- **复位撞角选错角**：按屏幕方位设 `MOUSE_HOME_CORNER`（右上→`CORNER_TOP_RIGHT`、左上→`CORNER_TOP_LEFT`、右下→`CORNER_BOTTOM_RIGHT`、左下→`CORNER_BOTTOM_LEFT`）。
- **落点需更贴中心**：分别微调 `MOUSE_HOME_BACK_X`（水平）与 `MOUSE_HOME_BACK_Y`（垂直），直至 KEY3 单次复位后光标停在中心附近。可按 **KEY3** 触发“停止并复位”现场确认落点。

---

## 6. 并发安全设计

鼠标任务与按键任务会**并发**调用 HID 发送接口。为避免竞态导致协议栈异常，采用 **统一发送队列 + 单一发送任务** 模型：

- 生产者（按键任务、动作引擎）调用 `ble_hid_send_mouse()` / `ble_hid_send_mouse_full()` / `ble_hid_send_key()`，将报文结构体投递到 FreeRTOS 队列。
- 唯一的 `hid_sender_task` 串行消费队列，调用 `esp_hidd_dev_input_set()` 发送给主机。
- 普通发送保持**非阻塞**：队列满（蓝牙拥塞）时丢弃该帧并告警，避免生产者任务堆积卡死（兼容原语义）。
- 动作引擎的**分片位移 / 鼠标复位**使用阻塞投递 `ble_hid_send_mouse_full_blocking()`（队列满时最多等待 100ms），保证连续位移的完整性与轨迹准确。
- 发送任务出队时再确认一次连接状态，断开瞬间残留报文被直接丢弃。

相比互斥锁方案，队列模型的好处：生产者不被协议栈阻塞；拥塞时可优雅降级。

---

## 7. 构建与烧录注意点

- 启用 Bluedroid 后固件约 1.1~1.4MB；sdkconfig 按 4MB Flash 配置（兼容 N16R8 16MB），使用自定义 OTA 分区表（`main/partitions_ota.csv`，含两个 app 槽位），避免烧录溢出。
- 修改 `sdkconfig.defaults.esp32s3` 后，因工程根目录已存在旧 `sdkconfig`，需**删除 `sdkconfig`** 或执行 `idf.py fullclean` 后重新构建，否则分区配置不会生效。

---

## 8. Windows 配对使用步骤

1. 给 ESP32-S3 上电，串口出现"等待新设备配对"提示。
2. 打开 Windows **设置 → 蓝牙和其他设备 → 添加蓝牙或其他设备 → 蓝牙**。
3. 选择 `BLE_KM`（即设备广播名），系统自动完成 Just Works 配对。
4. 配对成功后，设备同时出现在"鼠标"和"键盘"设备列表中。按 **KEY2** 启动随机动作模拟，按 **KEY0** 停止，状态 LED 在执行时点亮。
5. 如需重新配对其他电脑：在 ESP32 上 **Key3 + Key1 同时长按 3 秒** 触发重置，再于新电脑上重新添加设备。

---

## 9. 验收标准

| 项目 | 标准 |
| --- | --- |
| 设备识别 | Windows 配对后同时识别为"鼠标 + 键盘"，无驱动 |
| 自动重连 | 重启 ESP32 后自动重连已配对电脑 |
| 默认停止 | 上电后不执行任何动作，需按 KEY2 才启动 |
| 随机动作 | 启动后按概率执行 8 类动作；整体相对比例约 0/50/50/30/35/50/0/0（权重合计不必为100，仅看相对比例；拖拽、打字、切换程序默认权重0=不参与抽取） |
| 启停控制 | KEY2 启动、KEY0 立即停止；停止后按 KEY2 可恢复 |
| 停止释放 | KEY0 停止时鼠标左键与键盘按键均被强制释放，无卡死残留 |
| 蓝牙重置 | Key3+Key1 长按 3 秒后换地址重新广播，旧配对不再重连风暴 |
| LED | 动作执行中 GPIO1 常亮（低电平），停止时熄灭（高电平） |
| 日志 | 连接/断开/启动/停止/抽中动作/异常均有可观测串口输出 |
