# 动作说明

> 本说明对应固件实际实现，与 `main/action_engine.h` 宏定义、Web 控制台「动作参数」默认值保持一致。
> 概率权重代码用 `esp_random() % total` 取模，合计不必为 100，仅相对比例决定分布；权重 0 表示该动作不参与随机抽取（拖拽 / 打字 / 切换程序 / 写文本默认禁用）。

## 公共子过程

### 鼠标复位（仅在「滑动鼠标」动作开始前执行一次）

> **复位规则（当前版本）**：只有「滑动鼠标」（动作 6 `ACT_MOVE`）在**开始前**执行一次鼠标复位；其余所有动作（拖拽 / 点击 / 滚轮 / 方向键 / 休息 / 打字 / 切换程序 / 写文本）**都不再做鼠标复位**。
> 另外，`KEY3` 短按的「停止并复位」仍会手动执行一次复位（供确认落点），不受此规则影响。

BLE 鼠标是相对位移协议，读不到光标绝对位置；且 Windows 对 HID 相对位移有指针加速，按“半屏几何”回中心不可靠（推 / 回不抵消）。故复位采用**经验值两步法**（不依赖撞角前的光标位置）：

1. **撞角**：朝 `MOUSE_HOME_CORNER` 配置角落方向发出 `MOUSE_HOME_PUSH_PX`（默认 **2000**，分片，单帧 ≤25 计数、帧间 `ACTION_HOME_STEP_MS`=**12ms** 可中断），只要足够大，真实光标越过屏幕角落边界、被系统截停在角落（绝对位置已知）；
2. **回中心**：朝撞角反方向发出 `MOUSE_HOME_BACK_X`（默认 **400**）/ `MOUSE_HOME_BACK_Y`（默认 **200**，X/Y 独立，分片），决定最终落点。撞角余量已在角落被截掉，故 BACK 不必等于 PUSH 的一半，需实测收敛。

- 复位完成后逻辑坐标 `s_cur` 归零（仅用于后续动作取点，不影响复位落点）。
- 可用 `KEY3` 短按（停止并复位）或 `KEY1+KEY3` / `BOOT` 长按触发复位，现场确认落点是否在屏幕中心附近。

### 鼠标活动范围限制（全程生效）

逻辑光标坐标以屏幕中心为原点，全程钳制在矩形范围内（默认 ±**400** / ±**200** 像素，按 `SCREEN_SCALE_PCT`=**125%** 折算后有效范围 ±320 / ±160）。移动采用分片步进（单帧 ≤25 计数、帧间 `ACTION_MOVE_STEP_MS`=**28ms** 可中断），绝不瞬移。

---

## 动作列表（9 类，按当前默认权重）

### 1. 模拟鼠标拖拽（默认权重 0）

- 按下鼠标左键；
- 在活动范围内随机抽目标点，分片步进移动过去（方向随机，单帧位移 ≤25 计数，帧间 28ms）：
  - 位移距离：20 ~ 100（逻辑像素）；分片步进：10 ~ 30（每帧位移量）；
- 重复 **1 ~ 5 次**，每次间隔 **600 ~ 1500ms**；
- 松开左键；
- 结束延迟 **500 ~ 5000ms** 后启动下一个动作（**不复位鼠标**）。

### 2. 模拟鼠标点击（默认权重 50）

- 在活动范围内随机抽目标点、分片步进移过去；
- 单击左键（按下保持 **20 ~ 250ms** 后松开）；
- 重复 **1 ~ 10 次**，每次间隔 **100 ~ 1000ms**；
- 结束延迟 **1000 ~ 5000ms** 后启动下一个动作（**不复位鼠标**）。

### 3. 模拟鼠标滚轮（默认权重 50）

- 随机移动到目标点（分片步进）；
- 随机上下滚动鼠标滚轮（随机方向，每次步进 **1 ~ 8**）；
- 重复 **1 ~ 5 次**，每次间隔 **100 ~ 500ms**；
- 滚轮归位（发送 `-wheel_sum`，确保累计和为 0）；
- 结束延迟 **1000 ~ 5000ms** 后启动下一个动作（**不复位鼠标**）。

### 4. 模拟键盘方向键（默认权重 30）

- 随机敲击键盘上 / 下 / 左 / 右方向键（按下保持 10ms 松开）；
- 重复 **1 ~ 20 次**，每次间隔 **50 ~ 800ms**；
- 结束延迟 **1000 ~ 5000ms** 后启动下一个动作。

### 5. 模拟休息（默认权重 35）

- 随机延迟 **1 ~ 20 秒**（默认值 10 ~ 200）后启动下一个动作。
- 时间参数单位为 **×100ms**（如 `60` = 6 秒），在 Web 控制台「动作参数 → 休息」中可调。
- **连续休息上限**：`rest_max_total_sec`（秒，默认 **540 = 9 分钟**，0=不限）。连续休息累计超过该值后，若再次抽到休息，会被**强制改为随机执行一个非休息动作**（避免长时间空转）；并且**正在进行的这一次休息一旦累计超限会立即被打断**（休息按 100ms 分段累计/检查，无需等本次休息走完）。该计时**只在执行非休息动作时才清零**，只休息不做事会持续累加。

### 6. 模拟滑动鼠标（默认权重 50）

- 随机方向滑动随机距离（分片步进，单帧 ≤25 计数，帧间 28ms）：
  - 位移距离：10 ~ 30（逻辑像素）；分片步进：1 ~ 15（每帧位移量）；
- 重复 **1 ~ 20 次**，每次间隔 **100 ~ 500ms**；
- **动作开始前**执行一次「鼠标复位」（本动作是唯一会复位鼠标的动作）；
- 结束延迟 **500 ~ 1000ms** 后启动下一个动作（结束时不再复位）。

### 7. 打字（随机单词）（默认权重 0）

- 从单词表随机抽词，逐字符发送（字符间延迟 **40 ~ 700ms**），每词后加一个空格（空格延迟 **40 ~ 1000ms**）；
- 词数 **1 ~ 5**，词间间隔 **500 ~ 2000ms**；
- 结束延迟 **500 ~ 1200ms** 后启动下一个动作。

### 8. 切换程序（Alt+Tab）（默认权重 0）

- 按住 Alt，点按 Tab 切换窗口（Tab 按下保持 10ms）；
- 切换次数 **0 ~ 1**（0 = 本次不切换程序，等价于空转一次）；每次按键间隔 **500 ~ 1000ms**；
- 动作后休息 **700 ~ 1500ms**；
- 三组参数（切换次数 / 按键间隔 / 动作后休息）在 Web 控制台「动作参数」卡片中可调，页面输入上限已放宽（次数 0~999、间隔 50~5000ms、休息 50~60000ms）。

### 9. 写文本（默认权重 0）

把用户在 Web 控制台「📝 文本输入（写文本）」中配置的一整段文本（可上传 TXT，最多 **128K** 字符）**按字符逐个**转为键盘按键发送：

- **前置定位**：每轮写文本前，先连发 **若干次 PageDown**（默认 **16**，可在 Web 控制台「写文本」时间组中设置 `text_pre_pagedown_count`；按键保持 10ms、间隔 20ms），把目标文档光标移到末尾，确保新内容追加在文末。**不再发送 End 键**。
- 文本存放于独立 Flash 分区 `textdb`（不进 NVS），运行时读入 PSRAM 缓冲；首次启动若分区为空，自动写入内置默认文本（工程根 `Note.txt`，构建期由 `gen_default_text.py` 生成）。
- 支持的字符：**标准美式键盘可打印 ASCII（0x20~0x7E）全部字符**——字母、数字、空格及所有符号（含 `! @ # $ % & * ( ) _ + { } | : " ~ < > ?` 等 Shift 组合，自动处理 Shift），以及**回车换行**（`\n`→Enter）、**制表符**（`\t`→Tab）；非美式键盘可直接输入的字符（如中文）自动跳过。
- **每轮随机输出字符数**：在 `text_char_limit_min ~ text_char_limit_max` 区间随机取一个上限 N，本轮最多输出 N 个字符：
  - **达上限也不截断行**：即使已输出 N 个字符，也会继续补完**当前这一行**（直到遇到换行或文本末尾）才结束本轮，保证不会把一行文字打断在中间；
  - 本轮结束后转去执行其它动作；**下次回到本动作时从上次停下的位置继续**（输出位置保存在内存，断电/重启不保留）；
  - 输出到文本**末尾**则视为本轮完成，并**自动从头开始**（游标归零）；
  - 在 Web 上**修改/替换文本并保存**后，游标也会归零，下次从头输出。
  - **位置显示**：Web 页脚「设备状态」栏会显示「文本位置」= `当前位置 / 文本总长`（例如 `1234 / 40099`），每秒随状态刷新。
  - **手动指定起点**：「📝 文本输入（写文本）」文本框下方有「起始文档位置」输入框 + 「设为起点」按钮（默认 **0** = 从头）。填入字符下标并点击后，写文本的输出起点即改为该位置（调用 `POST /api/text/cursor`）。该值**仅存内存、不持久化**（重启后回到 0），与页脚「文本位置」同源；点「保存文本」后会归 0（因保存文本会复位游标）。
- 字符间延迟 **40 ~ 300ms**；遇换行处延迟 **80 ~ 600ms**；本轮结束后延迟 **500 ~ 2000ms**。
- **可选：屏蔽行首缩进**（`text_skip_line_indent`，默认 0=关）：开启（设为 1）后，**回车之后、出现首个非空白字符之前**的空格/制表符**不发送**。用于规避 VSCode 等编辑器“回车自动缩进”与文本自带缩进叠加造成的重复缩进。可在 Web 控制台「写文本」时间组中设置。
- 动作可像「打字」一样进入随机权重抽取与序列编排；默认权重 0（禁用），需在页面开启。

---

> 每个动作内部各自包含“动作内随机延迟”，动作结束后回到主循环，再追加 **100ms 保护延迟**（`ACTION_GUARD_DELAY_MS`）才抽下一个动作。休息与其他动作地位平等，都在 `pick_action()` 抽取范围内，且休息的 1~20s 延迟就在 `act_rest()` 内部。
